import styled from 'styled-components'

const Card = styled.div`
  border: 1px solid;
  border-radius: 20px;
  padding: 10px 20px;
  margin: 10px;
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
`;

export default Card;